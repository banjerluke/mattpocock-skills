---
"mattpocock-skills": patch
---

`to-spec`: every Implementation Decision now opens with the behaviour number(s) it serves, and factual claims about the codebase (where something lives, why it behaves as it does today) move out of the decisions into a new Believed Context section that implementers verify rather than honour. A spec that mixed the two told implementers a wrong file location was settled and cost them the time spent reconciling the spec with the code instead of correcting it.
